#!/bin/bash

#!/bin/bash
size=10
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done
size=100
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done
size=1000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done
size=10000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

size=20000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

size=40000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

size=80000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done
size=100000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done
size=200000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

size=400000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

size=800000
for i in 1 2 3 4
do
    # echo $number
    yes="graph-${size}-${i}.txt"
    echo $yes
    ./main $yes
    # ((number++))

done

